# 🤖 Bragent - AI Browser Agent

Автономный AI-агент для управления веб-браузером. Выполняет сложные многошаговые задачи без постоянного участия пользователя.

![Demo](docs/demo.gif)

## ✨ Возможности

- **Автономная работа** — агент сам принимает решения и адаптируется
- **Persistent Sessions** — можно войти в аккаунты вручную, агент продолжит работу
- **Умное извлечение контента** — не отправляет весь DOM, а только важные элементы
- **Security Layer** — спрашивает подтверждение перед опасными действиями
- **Красивый UI** — терминальный интерфейс в стиле Soft UI / Neomorphism

## 🚀 Быстрый старт

### 1. Установка

```bash
cd Bragent
npm install
```

### 2. Настройка

#### 🆓 Бесплатный вариант (GPT4Free)

```bash
# Установи gpt4free
pip install g4f[all]

# Запусти сервер (в отдельном терминале)
python -m g4f --port 8080
```

Создай `.env`:
```env
AI_MODEL=g4f/gpt-4o-mini
```

#### 💳 Платный вариант

```env
# Anthropic (Claude)
ANTHROPIC_API_KEY=ваш_ключ
AI_MODEL=claude/claude-3-5-sonnet-latest

# Или OpenRouter
OPENROUTER_API_KEY=ваш_ключ
AI_MODEL=anthropic/claude-3.5-sonnet
```

### 3. Запуск

```bash
npm run dev
```

## 🎯 Примеры задач

### Удаление спама из почты
```
Прочитай последние 10 писем в яндекс почте и удали спам
```

### Заказ еды
```
Закажи мне BBQ-бургер и картошку фри на Яндекс Еде из ближайшего ресторана
```

### Поиск информации
```
Найди на hh.ru 5 вакансий Python разработчика в Москве с зарплатой от 200к
```

## 🏗 Архитектура

```
src/
├── agent/
│   └── core.ts          # AI Agent с function calling
├── browser/
│   └── controller.ts    # Playwright управление браузером
├── context/
│   └── manager.ts       # Сжатие и форматирование контента
├── security/
│   └── layer.ts         # Проверка опасных действий
├── ui/
│   └── cli.ts           # Терминальный интерфейс
├── types/
│   └── index.ts         # TypeScript типы
└── index.ts             # Точка входа
```

## 🛠 Технологии

- **Playwright** — автоматизация браузера с persistent context
- **OpenRouter API** — доступ к Claude/GPT моделям
- **TypeScript** — типобезопасность
- **Chalk/Ora/Boxen** — красивый терминальный UI

## 🔒 Безопасность

Агент автоматически определяет опасные действия и запрашивает подтверждение:

- 🔴 **Критический** — оплата, перевод денег
- 🟠 **Высокий** — удаление, отмена подписки
- 🟡 **Средний** — подтверждение форм
- 🟢 **Низкий** — обычная навигация

## 📋 Конфигурация

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `AI_MODEL` | Модель AI | `g4f/gpt-4o-mini` |
| `ANTHROPIC_API_KEY` | API ключ Anthropic | — |
| `OPENROUTER_API_KEY` | API ключ OpenRouter | — |
| `MAX_ITERATIONS` | Макс. итераций на задачу | `30` |
| `BROWSER_HEADLESS` | Скрытый режим | `false` |
| `BROWSER_SLOW_MO` | Задержка между действиями (мс) | `50` |
| `USER_DATA_DIR` | Папка для данных браузера | `./browser-data` |

## 🎨 Поддерживаемые модели

### 🆓 GPT4Free (БЕСПЛАТНО!)
Запусти сервер: `pip install g4f[all] && python -m g4f --port 8080`
- `g4f/gpt-4o-mini` (рекомендуется)
- `g4f/gpt-4o`
- `g4f/gpt-4.1`
- `g4f/deepseek-v3`
- `g4f/claude-sonnet-4`

### Anthropic (платно)
- `claude/claude-3-5-sonnet-latest`
- `claude/claude-3-opus-20240229`

### OpenRouter (платно)
- `anthropic/claude-3.5-sonnet`
- `openai/gpt-4-turbo`
- `openai/gpt-4o`

### ZhipuAI (платно)
- `zhipu/glm-4-plus`
- `zhipu/glm-4-flash`

## 📝 Как это работает

1. **Извлечение контекста** — агент анализирует страницу: интерактивные элементы, формы, ссылки, текст
2. **Принятие решения** — AI определяет следующее действие на основе задачи и контекста
3. **Выполнение** — действие выполняется в браузере
4. **Адаптация** — если что-то не работает, агент пробует другой подход
5. **Завершение** — агент сообщает результат выполнения

## 🤝 Взаимодействие с пользователем

- Агент может задавать уточняющие вопросы
- Перед опасными действиями спрашивает подтверждение
- Пользователь может войти в аккаунты вручную — сессии сохраняются


